pipeline {
    agent {
        label 'any'
    }
    stages {
        stage('Build') {
            steps {
                container('any') {
                    sh 'ls -l'
                    sh 'date'
                }
            }
        }      
    }
}